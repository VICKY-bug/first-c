#include<stdio.h>
int my_strlen(char*str)
{
	int count=0;
	while(*str!='\0')
	{
		*str++;
		count++;
		
	}
	return count;
}
main()
{
	char arr[]="bit";
	int len=my_strlen(arr);
	printf("len=%d",len);
}
